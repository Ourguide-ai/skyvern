# Mermaid render test

If the block below shows two boxes with an arrow (not code text, not an error),
your Markdown **preview** renders Mermaid correctly.

```mermaid
sequenceDiagram
    Alice->>Bob: hello
    Bob-->>Alice: hi
```
